# Assets

- `logo.svg` — offizielles FITARY-Logo hier ablegen. Die App erkennt die Datei automatisch
  und ersetzt damit das Platzhalter-Monogramm in der Seitenleiste.
  Für PNG: `src` in `index.html` auf `assets/logo.png` ändern.
